{
  "private": true,
  "scripts": {
    "start": "dotnet tool restore && dotnet fable watch src/App.fsproj -s --run vite",
    "debug": "dotnet tool restore && dotnet fable watch src/App.fsproj -s --run npx vite",
    "build": "dotnet tool restore && dotnet fable src/App.fsproj --run vite build",
    "clean": "dotnet fable clean src/App.fsproj --yes"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "vite": "^6.4.1"
  },
  "overrides": {
    "ts-lsp-client": "1.0.4"
  },
  "engines": {
    "node": ">=18"
  }
}
